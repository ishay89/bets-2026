interface:
  display_name: "Codex Open Bet Predictions"
  short_description: "Automatically apply Codex-only open bet picks."
  default_prompt: "Check the current open published bets, factor in Codex's position, odds, and latest team news, choose Codex picks, upsert them for Codex only without waiting for approval, and verify the saved rows. Stop before writing only if I explicitly ask for read-only analysis or approval review."
