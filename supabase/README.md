# Supabase Setup

Run the migrations in `migrations/` in order in the Supabase SQL Editor (Dashboard -> SQL Editor).

The latest migrations create all tables, enable Row Level Security, and seed automated baseline users:

- Monkey: random reproducible picks
- Always Max: highest-odds pick
- Always Mid: median-odds pick
- Always Min: lowest-odds pick
