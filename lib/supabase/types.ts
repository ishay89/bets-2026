// Run `npm run gen:types` to replace this with your real Supabase schema.
// Until then, Database = any preserves the current runtime behaviour while
// wiring the generic into all three client factories.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
